# 2026-04-13 - Memory Log

## Completed Tasks
- **Web3 Wallet Login Integration**:
  - Updated `schema.prisma` with `walletAddress`.
  - Executed `prisma db push`.
  - Created `/api/auth/web3` login endpoint.
  - Updated `src/app/login/page.tsx` with "Sign in with Wallet" button.
- **Dashboard & Settings**:
  - Created `src/app/dashboard/settings/page.tsx`.
  - Added APIs for settings and wallet linking (`/api/user/settings`, `/api/user/link-wallet`).
- **Payment & QRIS**:
  - Added QRIS image to `src/app/payment/page.tsx`.
  - Updated price to Rp 250.000.
  - Linked "Upgrade Now" button to WhatsApp confirmation.
- **Localization**:
  - Implemented `src/translations/en.json` and `src/translations/id.json`.
- **Auth Fixes**:
  - Added "Forgot Password" feature with simulation API `/api/auth/reset-password`.

## Next Steps
- Visual verification of QRIS in live environment.
- End-to-end testing for Web3 login.
- Ensure Market Ticker displays real-time US stocks (AAPL, NVDA, etc.).

## Important Notes
- Users must link wallet in Settings before Web3 login.
- DATABASE_URL is verified and synced.
- Admin manual upgrade available via `/api/admin/upgrade-user`.
