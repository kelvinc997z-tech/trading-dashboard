
**Fix Verified (2026-04-09 22:15):**
- Updated `yahoo-finance.ts` to properly handle futures/ETF symbols (skipping `.US` for symbols with `=` or specific symbols like `SLV`).
- Updated `signal-updater.ts` to use Yahoo Finance as the primary source before falling back to Coinglass.
- Triggered `/api/market-signal/generate` and confirmed prices:
  - WTI (Oil): ~$102.55 (Real price)
  - XAG (Silver): ~$67.82 (Real price)
- System is now fully synced with Yahoo Finance as requested.
